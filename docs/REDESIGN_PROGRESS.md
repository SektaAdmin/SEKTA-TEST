# REDESIGN_PROGRESS — перехід на Vercel Geist design system

Масштабна поетапна задача. UI має стати як **Vercel Dashboard**: щільність, читабельність, без брендової емоційності (свідома відмова від червоного неону SEKTA; `--accent: #000`). Робиться посесійно: **1 сесія = 1 захід → коміт у `main` → Vercel деплой → скрін-перевірка 3 ролей**. (Гілки на сесію скасовано 2026-07-03 — усе прямо в main, див. «Ритм кожної сесії».)

**Рішення:** тёмна тема НЕ потрібна — токени одиночні, не парами light/dark.

Джерело істини по значеннях/шкалах — `DESIGN.md`. Історія й технічні перевірки (шрифт, кирилиця, PDF) — memory `project_redesign_vercel`.

## Джерело дизайну — `docs/geist/` (витягнуто з повного дампу Geist)

**Ми НЕ вигадуємо значення на око — ми копіюємо їх з автентичного Geist.**

**🔑 Нове джерело (з 2026-07-03): `Vercel_raw/geist-docs/` → `docs/geist/`.** Юзер зберіг **повний дамп** усього vercel.com/geist (78 сторінок компонентів, гідратований HTML + 3 CSS-бандли). На відміну від старого `Vercel_DS/` — тут розмітка компонентів **Є в HTML** (server-rendered/hydrated), тож `data-slot`/класи дістаються напряму, без копання в JS-чанках (див. готчу нижче — вона знята). Дамп великий і gitignored, тож з нього **одноразово згенеровано легкі markdown-довідники** у `docs/geist/` (комітяться, ~350 КБ проти 23 МБ сирцю):

- **`docs/geist/tokens.md`** — 405 токенів (колір light/dark у hex, spacing, radius, shadow, typography, motion). Ґенерує `scripts/geist/extract-tokens.mjs` (grep CSS-бандлів).
- **`docs/geist/components/<name>.md`** — 77 компонентів: розділи сторінки + DOM-контракт (`data-slot`→класи) + варіанти (згруповані за формою) + крос-реф токенів. Ґенерує `scripts/geist/extract-components.mjs`.
- **`docs/geist/theming.md`** — themed-система варіантів (кольори компонентів): `.geist-new-<color>` × модифікатор (`base`/`-fill`/`-contrast`/`-dark`) × light/dark → `--themed-bg/fg/border`. **Закриває кольори 🟡 «тонких» компонентів** (Button/Toast/Note/Alert…). Ґенерує `scripts/geist/extract-theming.mjs`.
- **`docs/geist/README.md`** — індекс з чесним покриттям: 🟢 структура/варіанти з HTML · 🟡 «тонкий» (кольори варіантів — у `theming.md`, структура — single-instance utility-класи в HTML).

**Робочий цикл звірки (з Сесії 3):**
1. Дивлюсь `docs/geist/components/<Компонент>.md` (НЕ сирий HTML — це вбиває ліміт). Там автентичні класи Geist.
2. Значення класів (gray-400, py-2.5, rounded-full…) → шукаю в `docs/geist/tokens.md`.
3. Оцифровую в токени `globals.css` + примітив; фіксую в `DESIGN.md`.
4. **Компонент 🟡 «тонкий»?** Кольори його варіантів — у `docs/geist/theming.md` (`.geist-new-<color>` → `--themed-*`). Структуру (розмір/радіус/відступи) бери зі single-instance utility-класів у HTML сторінки. Значення токенів — у `tokens.md`.
5. Регенерація за потреби: `npm run geist:docs` (tokens + theming + components).

**`Vercel_raw/` і `Vercel_DS/` — gitignored, НЕ комітити.** Якщо потрібного компонента нема в `docs/geist/` — перегенерувати зі скрипта; якщо нема в дампі — попросити юзера підвантажити, не апроксимувати.

**✅ Готчу знято:** у старому `Vercel_DS/` демо рендерились клієнтом, тож розмітки в HTML не було — доводилось копати cva з JS-чанків. У повному дампі `Vercel_raw/geist-docs` HTML **гідратований**, розмітка є → Table (`data-slot` th `h-10 px-2 font-medium`, td `px-2 py-2.5`, hover `bg-gray-100`), Badge (3 розміри × 17 варіантів fill/subtle/outline), StatusDot (`size-2.5 rounded-full`) знято прямо з HTML. themed-компоненти (Button/Select/Modal/Tabs/Toast…) тримають кольори варіантів у CSS, не в класах HTML → їхні кольори витягнуто окремим CSS-проходом у `theming.md` (🟡 у `.md` = структура з HTML + посилання на `theming.md`).

## Принцип

Спочатку **примітиви** (токени → кнопка/поле/таблиця/бейдж/модалка), потім **сторінки** як збірка з готових деталей. Не «сторінка за сторінкою» з нуля — інакше буде 8 різних «Geist».

## Ритм кожної сесії

1. **Референс:** юзер підвантажує потрібні Geist-сторінки в `Vercel_DS/` (перелік — у рядку кожної сесії нижче, поле «📥 Geist»).
2. **Працюємо прямо в `main`** — жодних гілок на сесію (рішення 2026-07-03: Сесії 0–4 стекувалися в гілках, це виявилось незручним; усе злито в main, далі коміти одразу в main → авто-деплой Vercel з main).
3. Звірка з бандлом (`grep Vercel_DS/`) → правки → `build` → скріни до/після 3 ролей (`review:browser`, моки rest/v1).
4. `commit` + push у `main` → Vercel деплой.
5. Відмітка тут (`[x]`) + оновлення `DESIGN.md` з приміткою про звірку.

---

## Фаза A — Фундамент

Правки в токенах зачіпають усі екрани одразу → скрін-перевірка всіх ролей обов'язкова.

- [x] **Сесія 0 — Оцифровка Geist у токени.** Значення звірено з CSS-бандлом vercel.com/geist (юзер підвантажив `Vercel_DS/` — НЕ комітити). Geist gray = чисто ахроматичний (HSL 0,0%). Тепле off-white → нейтральні сірі, кілька токенів лягли точно в Geist: `--bg #fafafa` = `--ds-background-200`, `--bg-2 #fff` = `--ds-background-100`, `--bg-3 #eeede9→#f2f2f2` = gray-100, `--text #1a1917→#171717` = gray-1000, `--accent-hover →#171717`. Радіуси (Geist materials: base/small=6, medium=12, fullscreen=16): `--radius 10px→12px` (Geist medium — контейнери), `--radius-sm=6px` (Geist base — контроли). Мертву `--brand-*` (0 споживачів) оцифровано в аутентичну рампу Geist gray. Семантичні кольори без змін. **Типографіка (докрутка Session 0):** юзер зберіг `vercel.com/geist/typography` (`Vercel_DS/Typography*`, gitignored) → grep класів `.text-heading/.text-copy/.text-label/.text-button` з CSS-бандла дав точну шкалу Geist (font-size/line-height/weight/tracking). Оцифровано в токени `--fs-*`/`--lh-*`/`--tracking-*` + ваги `--fw-normal/medium/semibold` (400/500/600): xs 12/16 · sm 13/18 · base 14/20 · md 16/24 · lg 20/26 (−0.4px) · xl 24/32 (−0.96px) · 2xl 32/40 (−1.28px). **Підпис Geist:** semibold-заголовки з негативним tracking (≈ −0.06×size), copy/label — normal. Токени лише ДОДАНО (не переписував наявні хардкоди font-size — це робота Сесій 1–11). `DESIGN.md` § Typography оновлено (шкала + правила). **Обмеження:** spacing-числа джерело ховає за Figma, але spacing-рампу Geist уже дістав з Grid-бандла (див. memory). type-scale — закрито. **Доступність:** середні сірі Geist (700 #8f8f8f=3.0:1, 800 #7d7d7d=3.5:1) НЕ проходять AA на дрібному тексті → текст лишається на `--text/-2/-3` (темніші за Geist mids). Синхронено: inline-bg `app/layout.tsx`, коментар `client.module.css`, `DESIGN.md`. Шрифт Geist уже стояв (f1a8dc4). Гілка `redesign/session-0-tokens`.
- [x] **Сесія 1 — Кнопки + поля.** Система кнопок у `globals.css`, **звірено з автентичним Geist Button** (`Vercel_DS/Button`, класи `geist-new-*`): `.btn-primary` + `.btn-secondary`/`.btn-ghost`/`.btn-danger` (кожна самодостатня, одна класа) + модифікатор `.btn-sm`. Спільна база: `--control-h` (=Geist small 32px), `--radius-sm` (6px=`rounded-md`), 14/500, `--motion-fast`. **Модель Geist (4 варіанти):** primary=default (чорна заливка #000, hover світлішає); **secondary=outline** (білий фон + `1px --border` ≈ gray-400, hover gray-100) — це «Скасувати»; **ghost=tertiary** (прозорий, БЕЗ рамки у спокої, fill+рамка на hover); danger=error. **Disabled=приглушені кольори** (gray-100 фон + `--text-3` текст + `--border`), НЕ opacity. `ModalFooter` → глобальні класи (cancel→**secondary**, save→primary, danger→danger); `ModalFooter.module.css` видалено. Поля (`FormField`): padding 8/12, focus-glow `0 0 0 3px rgba(0,0,0,.08)`, `aria-invalid` → червона рамка + `--danger-dim` glow, errorHint → `--danger-text` (4.5:1 на 11px), disabled → `--border-strong` + `--text-3`. Хардкоди кнопок по сторінках НЕ мігровано — робота Сесій 4–11. `DESIGN.md` §Buttons/Inputs + frontmatter звірено. Гілка `redesign/session-1-buttons-fields`.
- [x] **Сесія 2 — Таблиця + бейджі + пагінація.** Автентичні значення з JS-чанків Geist (демо рендеряться клієнтом — див. готчу вище). **Table** (`.data-table`): thead БЕЗ заливки (лише нижня рамка), `th` normal-case 14/500 висота 40px (Geist `h-10 px-2 font-medium` — не uppercase-мікрокапс), `td` верт. 10px (Geist `py-2.5`), гориз. 12px (Geist `px-2`=8px borderless; у картці-обгортці 12px для дихання), hover-рядок `--bg-3`=Geist gray-100. НЕ переносив Geist `last:text-right` (у нас різні останні колонки). **Badge** (`.badge`): геометрія Geist sm — `rounded-full` (`--radius-full`), h20/px8/gap4, 11/500, `letter-spacing:0.2px`, `tabular-nums`. Кольори лишив (семантику заморожено в S0); 1px рамку лишив як задокументоване відхилення (Geist badge borderless) — щоб бліді Dim-заливки читались без ретинту. `--badge-radius` (мертвий) прибрано. **Status Dot** (новий примітив `.status-dot`+`-neutral/-success/-warning/-danger/-info`, `.status-dot-item`): Geist `size-2.5`=10px коло, label 14/gap8. **Pagination** (`components/ui/Pagination.tsx`): Geist має лише prev/next — номерний пейджер = наша композиція на кнопкових токенах S1; стрілки → lucide `ChevronLeft/Right`, `.btn` flex-center висота `--control-h`. DESIGN.md §Tables/Badges/Status Dot/Pagination оновлено. Хардкоди по сторінках НЕ мігровано (робота Фази B). Гілка `redesign/session-2-tables-badges`.
  📥 **Geist:** `Table`, `Badge` (+ `Status Dot`), `Pagination`. ✅
- [x] **Сесія 3 — Модалки + фільтри/чипи.** Джерело — `docs/geist/` (повний дамп; `Modal`/`Select`/`Menu`/`Tabs` — «тонкі» компоненти: структура single-instance в HTML, кольори/тіні в `tokens.md`/`theming.md`; контейнери модалки/поповера рендеряться в портал → значення взято з токенів `--ds-shadow-modal`/`--ds-shadow-menu`/`--ds-overlay-backdrop-color`). **Нові токени** `globals.css`: `--shadow-menu` + `--shadow-modal` (автентичні Geist layered-тіні, перший шар = `0 0 0 1px` ring ≈ `--border` → замінює CSS-рамку; alpha з бандла `#00000005/0a/0f`) та `--modal-backdrop` (Geist backdrop = background-200 `#fafafa` @ .8 — **світлий фрост**, окремий токен бо `--overlay-bg` живить споживачів-тіней HallModal/Combobox). **ModalShell:** бекдроп → `--modal-backdrop`, картка → `box-shadow: --shadow-modal` (прибрано `border`). **FilterSelect (`.fs-*`):** поповер → `--shadow-menu` + radius `--radius`(12) + `padding:4px`, пункти → `rounded-md`+8×10px+gap10, hover gray-100, **selected нейтральний** (`--accent-dim`+500, не кольоровий акцент — Geist `bg-gray-alpha-100`); ~36px замість Geist `h-10`(40) для щільності. **`.filterChip`:** доданий hover неактивного (`--border-hover`+Ink). `ModalFooter` вже на глобальних кнопках з S1 — без змін. DESIGN.md §Elevation/Modals/Filter Select/Filter Chips оновлено. ⚠️ **Найпомітніша зміна — світлий бекдроп** (замість темного scrim): звірити на превʼю. Хардкоди по сторінках НЕ мігровано (Фаза B). Гілка `redesign/session-3-modals-filters`.
  📥 **Geist:** `Modal`, `Select`, `Menu` (dropdown), `Choicebox`/`Tabs` (чипи-фільтри). ✅

## Фаза B — Сторінки

У Фазі B примітиви вже готові (звірені в A) — Geist-сторінки тут потрібні лише для **композиційних** патернів (layout, відступи між блоками, тулбари, порожні стани). Якщо новий примітив не потрібен — нову сторінку в `Vercel_DS/` можна не тягнути.

- [x] **Сесія 4 — 🎯 Пілот: /sales.** Найщільніша таблична сторінка — прогнав крізь неї всю Фазу A, замінив локальні реімплементації примітивів. Гілка `redesign/session-4-sales-pilot`.
  **Мігровано на примітиви:** `.expenseBtn` → `.btn-secondary`; кнопки обох confirm-діалогів → `.btn-secondary`/`.btn-danger` (видалено локальні `.expenseBtn`/`.btnCancel`/`.btnConfirmDel`, ~50 рядків CSS). **Confirm-діалоги** переведено на токени Сесії 3: бекдроп `--overlay-bg` → `--modal-backdrop` (світлий фрост), картка `border` → `box-shadow: --shadow-modal` (1px-ring у тіні). `.confirmError` колір `--danger` → `--danger-text` (4.5:1). **Прибрано мертві оверрайди:** `font-size: 14px !important` з клітинок `.date/.price/.deposit/.sessions/.opTopup/.opDeduction/.depositLabel` — базовий 14px тепер дає примітив `.data-table` (сторінка більше не дублює розмір).
  **Що вже було на примітивах (лише звірено, що тримає щільність):** таблиця `.data-table`, платіжні бейджі `paymentClass()`→`.badge-*`, `Pagination`, `FilterSelect`, mobile `.filterChip`.
  **Калібраційні знахідки (gaps Фази A) — лишено локальним, це НЕ борг:** (1) сегментні таби фіду (`.feedTab`/`.feedTabActive`) — у Фазі A нема tabs-примітива (Session 3 зробив лише chip); тримаються локально на токенах, активний стан нейтральний (`--accent-dim`). (2) inline-дії таблиці (`.btnEdit`/`.btnDel`/`.btnReceipt`) — навмисно локальні: спільна база на `--btn-sm-*`, але з семантичним hover (del→danger, receipt→accent), чого `.btn-*` не покривають; це свідомий патерн «тонкі inline-actions», не реімплементація. (3) `.filterSearchInput` (пошук з іконкою), `.sheetDone`/`.filterToggleBtn` (mobile-хром із `display:none`-на-desktop, застосування `.btn-*` крихке через конфлікт `display`) — лишено. tsc чистий; build не ганяв (dev на :3000, .next-готча) — скрін-перевірка на Vercel preview гілки.
  📥 **Geist:** `Grid` (вже є), за потреби `Input`/`Select` для тулбара фільтрів. ✅
- [x] **Сесія 5 — /accounting** (+ `/settings/salary/rates`, `/settings/salary/calculations`, `StudioExpenseModal`). `/accounting/trainers`, `/rates`, `/salary` виявились редіректами (нема власного UI) — реальний обсяг: `/accounting/page.tsx` + 2 сторінки зарплат.
  **/accounting:** вже здебільшого на примітивах Фази A (`.data-table`, `FilterSelect`, `paymentClass()`→`.badge-*`, balance-картки з семантичним кольором на реальний фінансовий сигнал). Прибрано мертвий CSS з попередньої версії сторінки (`.addExpenseBtn`/`.trainerReportLink` — старий тулбар без кнопки додавання, зараз `StudioExpenseModal` викликається лише з `/sales`; `.totalsBar`/`.summaryCard`/`.preset`/`.dateLabel`/`.thTrainer`/`.trainerCell`/`.clientCell`/`.topbarActions` — залишки старої верстки без React-споживачів), разом ~140 рядків.
  **`/settings/salary/rates` + `/settings/salary/calculations`:** локальні `.formField`/`.label`/`.select`/`.input`/`.fieldLabel`/`.modalInput` дублювали `FormField` але відстали (без focus-glow/aria-invalid/disabled-стилів Сесії 1) — мігровано всі текстові/select-поля модалок (додавання ставки, фіксація виплати) на `FormField`; видалено дублюючий CSS з `rates.module.css`/`calculations.module.css`. **Лишено локальним:** `.typeToggle`/`.typeBtn` (сегментний перемикач методу/типу виплати) і `.dirBtn`/`directionRow` у `StudioExpenseModal` — той самий патерн, що `.feedTab` на /sales (Сесія 4): Фаза A не дала tabs-примітив, тому це не борг. Inline-дії таблиць (`.editBtn`/`.archiveBtn`/`.restoreBtn`, settings.module.css) вже були на `--btn-sm-*` токенах — той самий свідомий "тонкий inline-actions" патерн з Сесії 4, без змін.
  tsc чистий; build не ганяв (dev на :3000, .next-готча) — скрін-перевірка `review:browser` (3 екрани admin: `07-accounting`/`08-salary-rates`/`09-salary-calc`) + ручна перевірка обох модалок (додавання ставки, фіксація виплати) в браузері.
  📥 **Geist:** з A; `Tabs`/`Description List` не знадобились — сторінки табличні, без нового композиційного патерну.
- [x] **Сесія 6 — /clients + /clients/[id]** (+ `ClientModal`).
  **`ClientModal`/`/clients` (list):** вже здебільшого на примітивах (`ModalShell`+`ModalFooter`+`FormField`+`SocialHandleInput`, `.data-table`, форма-помилка на `--danger`/`--danger-dim` еталоні SaleModal) — лише прибрано мертвий `.pageSizeSelect` з `clients.module.css` (клас без React-споживача, pagination рендерить власний select).
  **`/clients/[id]` (найбільший обсяг):** `client-profile.module.css` містив повний набір до-Фази-A кнопок і confirm-модалки. Мігровано: усі `.btnEdit`/`.btnPrimary`/`.btnConfirmCancel`/`.btnConfirmDel` (16 місць у JSX) → глобальні `.btn-secondary`/`.btn-primary`/`.btn-danger`; 3 локальні confirm-оверлеї (створення кабінету, скидання пароля, видалення продажу) → токени Elevation Сесії 3 (`--modal-backdrop` світлий фрост + `box-shadow: --shadow-modal`, без CSS-рамки) — той самий еталон, що `/sales` `.confirmOverlay` з Сесії 4. `.btnLoadMore` → `.btn-secondary` + тонкий локальний layout-модифікатор `.loadMoreBtn` (display:block+width:100%, бо `.btn-*` не покриває full-width).
  **Лишено локальним (свідомо, не борг):** `.btnRowEdit`/`.btnRowDel` (inline-дії таблиць) вирівняно **точно** під "тонкий inline-actions" патерн з `/sales` (`--btn-sm-*` база + семантичний hover del→danger) замість здогадування; `.tab`/`.tabActive` (таби Зведена стрічка/Продажі) — той самий gap tabs-примітива, що `.feedTab` на /sales (Сесія 4) і `.typeToggle` на /accounting (Сесія 5), Фаза A не дала tabs-примітив; `.iCard`-мобільні картки (Майбутні/Стрічка/Продажі) — власна композиція, нема еквівалента в Geist A.
  tsc чистий; build не ганяв (dev на :3000, .next-готча) — перевірка `review:browser` (`03-clients`) + ручний прохід Playwright: список, модалка додавання/редагування клієнта, профіль клієнта (контакти/депозит/залишок занять/постійні-майбутні записи/таби), кнопка «Створити кабінет» — усі кнопки на нових токенах.
  📥 **Geist:** з A; `Avatar`/`Description List`/`Tabs` не знадобились — сторінка композиційно табличная+картки, без нового патерну.
- [x] **Сесія 7 — /schedule** (+ `/schedule/templates`, `ClassDetailModal`, кабінет тренера `TrainerSchedule.tsx` — спільний CSS module).
  **`/schedule`:** топбар-кнопки (`.navBtn`/`.todayBtn`) → `btn-secondary` (розмір-модифікатор лишився локальним для квадратних 30×30); mobile-календар bottom-sheet (`.mobileCalOverlay`/`.mobileCalTodayBtn`) → Elevation Сесії 3 (`--modal-backdrop` світлий фрост + `.btn-secondary`). Прибрано мертвий CSS з попередніх ітерацій топбару: `.tabs`/`.tab`/`.tabActive` (нема споживачів у JSX), `.btnTemplates`/`.iconBtn`/`.viewLabel`, `.filterGroup`/`.filterDivider`/`.filterBtn*` (замінені FilterSelect ще раніше), уся `.archiveList`-родина (стара архівна сторінка, замінена `/journal`) — разом ~180 рядків. `.viewToggle`/`.viewBtn` (День/Тиждень) лишено локальним — той самий gap tabs-примітива, що `.feedTab` (/sales), `.typeToggle` (/accounting), `.tab` (/clients) з попередніх сесій.
  **`ClassDetailModal`** (найбільший обсяг — модалка деталей заняття, відкривається з усіх 3 ролей): footer-кнопки (Редагувати/Скасувати/Відновити/Видалити) → `.btn-secondary`/`.btn-danger`/`.btn-primary`/`.btn-ghost`; 3 confirm-діалоги (скасування/відновлення/видалення заняття) → Elevation Сесії 3 (`--modal-backdrop`+`--shadow-modal`, той самий еталон що `/sales`/`/clients/[id]`); inline row-actions таблиці записаних (Так/Ні, Видалити) → `.btnRowEdit`/`.btnRowDel` (той самий "тонкий inline-actions" патерн з `--btn-sm-*`, що `/clients/[id]`); таблиця (`.table th`) uppercase-мікрокапс → normal-case 14/500 h-40px (Geist Table); прибрано мертвий CSS `.btnAdd`/`.btnCancelAdd`/`.btnConfirmEnroll`/`.btnAttend`/`.btnNoshow`/`.btnCancelEnroll`/`.btnReEnroll`/`.actionSelect` (без React-споживачів — вже замінені `ActionSelect`-компонентом раніше).
  **`/schedule/templates`:** топбар (`.navBtn`, mobile+desktop) → `.btn-secondary`; діалоги "Виставити тиждень"/"Видалити розклад" footer-кнопки → `.btn-secondary`/`.btn-primary`/`.btn-danger`; тригер "Видалити розклад" → `.btn-ghost` (найтихіший, рідкісна небезпечна дія); список-таблиця (`.table th`) normal-case; inline row-actions (Редагувати/Видалити шаблон) → `.btnRowEdit`/`.btnRowDel`. Прибрано мертвий CSS: `.generateDialog`/`.generateLabel`/`.generateInput`/`.generateUnit` (MiniCalendar рендериться через `CalendarPopover`, не цей блок).
  **`CalendarPopover`** (спільний поповер вибору дати — templates generate/delete, ScheduleRightPanel calendar): border+`--shadow-md,--shadow-sm` → Elevation Сесії 3 `--shadow-menu` (ring замінює CSS-рамку, той самий еталон що `.fs-content`).
  **Кабінет тренера (`TrainerSchedule.tsx`)** зачеплено мінімально — той самий CSS module (`schedule.module.css`), тож `.navBtn`/`.todayBtn` синхронізовано з `btn-secondary`, щоб не зламати спільний стиль. Повний обсяг кабінету тренера — Сесія 9.
  tsc чистий; browser-review 18/18 (3 ролі) пройшли; вручну звірено ClassDetailModal (список записаних, footer-кнопки, confirm-скасування заняття) — Elevation/кнопки відповідають еталону.
  📥 **Geist:** з A; композиція власна (календарна сітка).
- [x] **Сесія 8 — /dashboard** (найвидиміший екран, фінал адмінки).
  Дашборд виявився вже здебільшого на токенах Фази A (5 блоків `_components/` вже писались проти `--text`/`--bg-3`/`--radius-sm`/`--border`) — обсяг менший, ніж попередні сесії. Знайдено й виправлено 4 конкретні відхилення: (1) заголовок сторінки `.title` (локальний дубль 20/600) → глобальний `.page-title` (той самий клас, що `/clients`/`/accounting`), клас видалено з `dashboard.module.css`; (2) `.debtTable th` — uppercase-мікрокапс 11px/600/letter-spacing → normal-case `--fs-base`/`--fw-medium`/`--text` (Geist Table, той самий еталон з Сесії 2), зберіг sticky/right-align/border — специфіку цієї нестандартної (не `.data-table`) таблиці; (3) `.blockErrorRetry` (локальна кнопка-реімплементація) → `.btn-secondary.btn-sm` у `BlockError.tsx`, клас видалено; (4) `.blockTitle` хардкоди 16px/500 → токени `--fs-md`/`--fw-medium` (значення ті самі, що вже було, тепер джерело — токен).
  **Лишено як є (не борг):** решта хардкоджених font-size (12/13/14/18px у `.slotChip`/`.spacesTime`/`.spacesCountNum`/`.debtType` тощо) — це навмисна семантична градація (14px операційний текст, 18/700 великий лічильник вільних місць, 12px найтихіший choreo-рядок), не відхилення від Geist-шкали; значення точно збігаються з `--fs-xs/sm/base/md`. `ClassDetailModal`, що відкривається з `FreeSpacesBlock`, вже мігровано в Сесії 7.
  tsc чистий; `review:browser` 18/18 (3 ролі) пройшли; скрін `/dashboard` звірено вручну — заголовок і таблиця боржників на нових токенах.
  📥 **Geist:** з A; композиція власна (немає нового патерну — блоки-картки вже існували).
- [x] **Сесія 9 — Кабінет тренера** (`/trainer`, `/schedule`, `/clients`, `/my`, `CabinetHeader`).
  **`BottomNav` не спільний** з кабінетами — виключно адмінська нижня навігація (окремий компонент); кабінети тренера й клієнта мають власне меню-лаунчер, тому ця сесія їх не зачепила.
  **`CabinetHeader`** (спільний з `/client`!): кнопки «‹ назад»/«Вийти» були піксель-в-піксель реімплементацією `.btn-secondary` (той самий height/padding/border/hover) → мігровано на глобальний клас (`.back`/`.logout` лишились лише як розмір-модифікатори, той самий патерн що `.navBtn`); `.title`(20/600)/`.logo`(13/600)/`.address`(13)/`.subtitle`(14) → токени `--fs-lg`/`--fs-sm`/`--fs-base`+`--fw-semibold` (той самий фікс, що дашборд у Сесії 8). Торкається і кабінету клієнта.
  **`TrainerHome.tsx`+`trainer.module.css`:** кнопка «Підключити» (Telegram-посилання) була локальною чорною заливкою-реімплементацією `.btn-primary` → мігровано. Видалено мертвий CSS-блок (~100 рядків: `.tabBar/.tab/.tabActive/.list/.item/.cancelled/.date/.day/.month/.info/.title/.meta/.choreo/.badge`) — залишки старої версії «Мої заняття» на головній, не використовувались жодним JSX.
  **`TrainerMyClasses.tsx`+`my.module.css`:** та сама мертва трійка `.tabBar/.tab/.tabActive` (дублікат, теж без споживачів) видалена.
  **`TrainerSchedule.tsx`+`schedule.module.css` (desktop):** 2 ряди чипів фільтра залів (топбар + окремий filterBar-рядок) були на inline `style={{...}}` (ручний padding/border/color) — дублювали глобальні `.filterChip`/`.filterChipActive`, вже застосовані в мобільній версії цього ж компонента й на адмінському `/schedule`. Мігровано на глобальні класи; новий локальний `.trainerFilterBar` лишає лише page-специфічний padding (той самий мінімалістичний патерн, що `.mobileTlHallChips` — не редеклue display/gap, які вже задає `.filterChips`).
  **Лишено локальним (не борг):** `.viewToggle`/`.viewBtn` (День/Тиждень) — той самий gap tabs-примітива з попередніх сесій; `.tgDisconnectBtn` (13px текст-лінк «Відключити») — не має еквівалента серед `.btn-*` (надто легкий); `.backBtn`/`.todayBtn` (my/clients) і `.trainerSchedBackBtn`/`.trainerSchedTodayBtn` (mobile topbar) — текст-лінк навігація без рамки/фону, не `.btn-*`-форма; `.topbarTitle`/`.trainerSchedTitle` 15px/600 — офскейл легасі-значення (не збігається з жодним `--fs-*` точно), лишено без здогадування замість тихого підняття до 16px; `ClientSessionsModal`/`CreateClientModal` вже були зразковими (ModalShell+ModalFooter+FormField) — без змін.
  tsc чистий; browser-review (Playwright, роль тренера): /trainer, /trainer/schedule (mobile 375px + desktop 1440px), /trainer/my, /trainer/clients — 6 скріншотів, без консольних помилок (крім наявної раніше непов'язаної `/sw.js`-помилки); чипи залів на десктопі підтверджено як пігулки з нейтральним активним станом, фільтрація робоча.
  📥 **Geist:** з A; mobile-навігація власна.
- [x] **Сесія 10 — Кабінет клієнта** (`/client`, `/schedule`, `/subscriptions`, `/visits`).
  Сторінка вже здебільшого писалась проти токенів (`--text`/`--bg-2`/`--border`/`--radius`) — `CabinetHeader` (спільний з тренером) мігровано раніше в Сесії 9. Обсяг цієї сесії — переважно **прибирання мертвого CSS**, накопиченого через кілька редизайнів екрана (список "Майбутні/Архів" → замінений на `ClientVisits` з `WeekStrip`; окремий "Профіль read-only" блок → замінений `.profileCard` на головній): `.hero`/`.heroLabel` (акцентна картка "наступне заняття", вже не рендериться), уся стара `.tabs`/`.tab`/`.tabActive`/`.list`/`.monthHeader`/`.monthList`/`.item`/`.cancelled`/`.date`/`.day`/`.month`/`.relDay`/`.info`/`.title`/`.meta` родина (~120 рядків), `.profile`/`.profileHeader`/`.profileLabel`/`.profileRow`/`.profileValue` (заміщені `.profileCard`), `.listFooterNote`, `.visitTimerPast`/`.visitTimerBottom`/`.visitTimerClassCancelled`, `.visitTrainer` (дубль без споживача — живий лише `.visitTrainerTop`), `.detailPrice`, `.confirmInfo`, `.bookBalanceBar` — разом 247 рядків.
  **Виправлено 2 інлайн-реімплементації:** error-банер у `ClientSchedule.tsx`/`ClientVisits.tsx` був `className="badge-danger" style={{padding,borderRadius}}` (сирий інлайн-стиль поверх семантичного badge-модифікатора) → замінено на вже наявний `styles.sectionError` (той самий словник, що використовує `ClientSubscriptions.tsx`).
  **Лишено локальним (не борг):** `.visitTab`/`.visitTabOn` (Майбутні/Історія) — той самий gap tabs-примітива, що `.feedTab`/`.typeToggle`/`.tab`/`.viewToggle` з попередніх сесій; `.bookTrainerChip`/`.bookTrainerChipOn` (фільтр тренера з аватаркою) — семантично інший компонент за `.filterChip` (кольорова заливка `--accent`, не нейтральний `--accent-dim`; додаткова аватарка) — не примітив Фази A; `.bookTodayBtn` — задокументовано вже як дзеркало адмінського `.todayBtn` (Сесія 7), не `.btn-secondary`; `.mapBtn`/`.cancelBtn`/`.showMore`/`.txShowMore` — повноширинні/danger-outline кнопки з layout-вимогами (width:100%), які `.btn-*` не покриває напряму — той самий патерн, що `.loadMoreBtn` у Сесії 6.
  tsc чистий; `review:browser` 4/4 client-екрани (`01-home`/`02-schedule`/`03-visits`/`04-subscriptions`) пройшли без консольних помилок, скріни звірено вручну.
  📥 **Geist:** з A; композиція власна (мобільний меню-лаунчер + тижнева стрічка, немає нового патерну).
- [x] **Сесія 11 — Settings + хвіст** (`/settings/*`, `/halls`, `/tickets`, `/training-types`, `/journal`, `/audit`, `/login`).
  Аудит показав: `/halls`, `/tickets`, `/training-types` (корінь) — чисті redirect-заглушки на `/settings/*` (як `/accounting/trainers` у Сесії 5), без власного UI. `/settings/*` (`_RefEntityPage.tsx` — спільний рушій для tickets/trainers/halls, + `training-types/page.tsx`) уже здебільшого на примітивах Фази A: `.data-table`, `.btn-primary`, `.page-title`, inline row-actions (`.editBtn`/`.restoreBtn`) вже на `--btn-sm-*` токенах (той самий "тонкий inline-actions" патерн з Сесій 4/6/7). `/journal` + `/audit` — уже повністю на токенах (`--border`/`--radius-sm`/`--text-2/3`/`--motion-fast`), `FilterSelect`, badges, `.data-table`; власна пагінація (перша/попер/наст/остання, без page-size-селектора) — свідомо відмінна від `Pagination`-компонента (Сесія 2 дає лише номерний пейджер), не борг.
  **Єдина реальна знахідка — `/login`:** єдина сторінка проєкту поза макетом `Sidebar`+`page-layout`, не займана жодною попередньою сесією, з до-Фази-A реімплементацією `.field`/`.label`/`.input`/`.btn` (дублювала `FormField`/`.btn-primary` без focus-glow/токенів). Мігровано: обидва поля → `FormField` (patrern з `HallModal` — `children`+`value`/`onChange`, без react-hook-form), кнопка → `.btn-primary` (локальний модифікатор `.form .btn-primary { width:100% }` — той самий патерн layout-модифікатора, що `.loadMoreBtn` у Сесії 6), `.error` колір → `--danger-text` (4.5:1 еталон), `.sub` розмір → `--fs-base`. `.logo` (22px/600 брендовий wordmark) лишено як свідомий одинак — не на шкалі `--fs-*`, як великий лічильник дашборду в Сесії 8.
  tsc чистий; dev-сервер живий → build не ганяв (готча); Playwright-скрін `/login` вручну звірено — картка, обидва поля з фокус-glow, повноширинна чорна кнопка.
  📥 **Geist:** з A; `Switch`/`Radio`/`Checkbox`/`Toggle` не знадобились — форми налаштувань уже на `FormField`, нового патерну немає.

## Фаза C — Полірування

- [x] **Сесія 12 — Фінальний прохід.** Наскрізна консистентність, видалення мертвого CSS, оновлення `DESIGN.md`.
  **Аудит (2 фонові дослідницькі агенти):** (1) tabs/segmented-toggle патерн — 14 реалізацій по репо, знайдено реальний **дрейф**: `/schedule` `.viewToggle` і `/schedule/templates` `.viewToggle` — той самий компонент (День/Тиждень), продубльований з різними назвами класів (`.viewBtn` vs `.toggleBtn`) і один без `font-weight:500`; (2) мертвий CSS — **не знайдено** (попередні сесії вже прибрали все), хардкод-токени — десятки `var(--danger, #d93535)` (мали бути просто `var(--danger)`), 2× `color:#fff` (мали бути `var(--white)`), 1 неправильний токен (`var(--error, #e53e3e)` в SeriesModal — `--error` не існує, мало бути `--danger-text`), 1 забутий модальний бекдроп (`/sales` mobile filter-sheet — `rgba(0,0,0,0.4)` замість `var(--modal-backdrop)`, хоча ідентичний mobile-sheet на `/schedule` уже мігровано в Сесії 7).
  **Нові примітиви `globals.css`:** `.tabs-segmented`/`.tab-seg`/`.tab-seg-active` (бордер-рядок кнопок, спільна зовнішня рамка) — замінив `.feedTab` (/sales), `.viewToggle`×2 (schedule+templates, дрейф закрито), лишились локальними `typeToggle`(salary/calculations)/`paymentTabs`(SaleModal) — інша геометрія (`flex:1` окремі пігулки з gap, не суцільна рамка), і `SaleModal` свідомо тримає суцільну `--accent`-заливку для вибраного методу оплати (грошовий вибір — навмисно вищий акцент, не недогляд). `.tabs-underline`/`.tab-underline`/`.tab-underline-active` (підкреслений таб-рядок) — замінив `.tab`/`.tabActive` на `/clients/[id]`; `.visitTab` кабінету клієнта лишився локальним (mobile-специфіка: 13px, `flex:1`, sticky — той самий підкреслений патерн, але навмисно інших пікселів під mobile-щільність). `/settings` boolean-перемикач і `ClassModal` padded-track picker — НЕ таби (інша семантика), не займались.
  **Хардкод-фікси:** ~12× `var(--danger, #d93535)`→`var(--danger)` (schedule/client/trainer-my), 2× `color:#fff`→`var(--white)` (trainer avatar, trainer/clients FAB), `SeriesModal` `--error`→`--danger-text`, `/sales` mobile sheet-overlay→`var(--modal-backdrop)`+blur (вирівняно під еталон `/schedule` Сесії 7).
  tsc чистий; Playwright-скрін-перевірка 4 сторінок (`/sales` feed-таби, `/schedule` День/Тиждень, `/schedule/templates` День/Тиждень/Список, `/clients/[id]` Зведена стрічка/Продажі) — нові класи застосовані коректно, візуал не зміг регресувати (значення примітивів = ті самі, що були локально), консоль без помилок.
  📥 **Geist:** без нових — фінальна звірка з уже завантаженим у `docs/geist/`.
