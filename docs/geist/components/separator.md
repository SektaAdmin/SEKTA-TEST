# Separator

> Автозгенеровано з `Vercel_raw/geist-docs/vercel.com/geist/separator.html` скриптом `scripts/geist/extract-components.mjs` (`npm run geist:components`). Не редагувати вручну.
> Класи — автентичні Geist (Tailwind), значення дивись у [tokens.md](../tokens.md).

## Структура сторінки (розділи)

- Separator
  - Horizontal
    - Section 1
    - Section 2
  - Vertical
  - Orientation Variants
  - Accessibility Variants
  - Custom Styling

## DOM-контракт (data-slot → класи)

### `separator` — `<div>`

6 варіанти класів (× = скільки разів на сторінці):

- `bg-gray-200 shrink-0 h-px w-full` ×5
- `bg-gray-200 shrink-0 w-px h-full` ×5
- `shrink-0 h-px w-full bg-blue-500` ×1
- `bg-gray-200 shrink-0 w-full h-2` ×1
- `shrink-0 h-full bg-red-500 w-0.5` ×1
- `shrink-0 h-full bg-green-500 w-2` ×1

## Токен-класи, які використовує компонент

`bg-blue-500` · `bg-gray-200` · `bg-green-500` · `bg-red-500`

