# Status Dot

> Автозгенеровано з `Vercel_raw/geist-docs/vercel.com/geist/status-dot.html` скриптом `scripts/geist/extract-components.mjs` (`npm run geist:components`). Не редагувати вручну.
> Класи — автентичні Geist (Tailwind), значення дивись у [tokens.md](../tokens.md).

## Структура сторінки (розділи)

- Status Dot
  - Default
  - Label
  - Best Practices
    - When to use
    - Behavior
    - Content
    - Accessibility

## Варіанти (утилітарні корені, згруповано за формою)

### Форма 1 (4 варіантів)

**База:**

```
inline-block size-2.5 rounded-full
```

**Відмінності (по одному рядку на варіант):**

- `bg-[var(--accents-2)]`
- `bg-[var(--geist-warning)]`
- `bg-[var(--geist-error)]`
- `bg-[var(--geist-cyan)]`

