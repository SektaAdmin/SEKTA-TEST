# Аудит страницы /sales — SEKTA-CRM (Обновлено)

**Дата:** 19 июня 2026  
**Страница:** `/sales` (продажи и студийные операции)  
**Результат:** **17/20 — Хорошо** (исправлены P0 и P2 находки)

---

## Таблица оценок (обновлено после исправлений)

| # | Измерение | Оценка | Ключевая находка |
|---|-----------|--------|------------------|
| 1 | Доступность (A11y) | 3 | WCAG AA в основном соблюдается, но табы без ARIA-ролей (ожидает `/impeccable harden`) |
| 2 | Производительность | 3 | Осталась одна анимация ширины (L65), прочие исправлены |
| 3 | Адаптивный дизайн | 4 | Отлично; правильный breakpoint 640px, touch-targets 44px+ |
| 4 | Система токенов | 4 | **✅ Исправлено**: удалены hardcoded fallback'и, все цвета из `--warning` / `--warning-dim` |
| 5 | Anti-Patterns | 3 | **✅ Исправлено**: боковые смуги заменены на фоновую подсветку (danger-dim / success-dim) |
| **Итого** | | **17/20** | **Хорошо** — осталась одна P1 (layout-transition) |

---

## Что было исправлено

### ✅ [P0] Боковые цветные полоски — РЕШЕНО

**Было (lines 115–116):**
```css
.cardExpense { border-left: 3px solid var(--danger) !important; }
.cardIncome  { border-left: 3px solid var(--success) !important; }
```

**Стало:**
```css
.cardExpense { background: var(--danger-dim); }
.cardIncome  { background: var(--success-dim); }
```

**Результат:** ✅ Абсолютный запрет Impeccable (боковые смуги > 1px) закрыт. Фоновая подсветка намного чище и профессиональнее.

---

### ✅ [P2] Hardcoded цвета warning — РЕШЕНО

**Было (lines 315, 319):**
```css
.depositWarning {
  color: var(--warning, #f59e0b);
  background: var(--warning-dim, rgba(245, 158, 11, 0.1));
}
```

**Стало:**
```css
.depositWarning {
  color: var(--warning);
  background: var(--warning-dim);
}
```

**Результат:** ✅ Fallback-цвета удалены (токены всегда определены в `globals.css`). Система теперь чистая и консистентная.

---

## Оставшиеся находки (требуют дальнейшей работы)

### [P1] Анимация ширины на фокусе инпута поиска

**Локация:** `app/sales/sales.module.css`, line 65  
**Категория:** Производительность (Layout Thrash)  
**Статус:** ❌ Требует исправления в `/impeccable optimize`

```css
.filterSearchInput {
  transition: border-color 0.12s, width 0.12s ease-out;  /* ← ширина! */
}
.filterSearchInput:focus { 
  width: 240px;  /* ← микро-реflow */
}
```

**Рекомендация:** Используйте `transform: scaleX()` вместо `width`.

**Команда:** `/impeccable optimize`

---

### [P1] Нет ARIA-ролей на табах фильтра

**Локация:** `app/sales/page.tsx`, lines 181–189  
**Категория:** Доступность (WCAG AA)  
**Статус:** ❌ Требует исправления в `/impeccable harden`

Табы без `role="tablist"`, `role="tab"`, `aria-selected`.

**Команда:** `/impeccable harden`

---

### [P1] Нет явных лейблов на input'ах фильтра

**Локация:** `app/sales/page.tsx`, lines 193–243  
**Категория:** Доступность (Связь форм)  
**Статус:** ❌ Требует исправления в `/impeccable harden`

Поисковый инпут и date picker'ы без явных `<label>` элементов.

**Команда:** `/impeccable harden`

---

## Сводка улучшений

**До исправлений (15/20):**
- ❌ P0 Боковые полоски (абсолютный запрет)
- ❌ P1 Анимация ширины
- ❌ P1 Нет ARIA-ролей
- ❌ P1 Нет явных лейблов
- ❌ P2 Hardcoded цвета

**После исправлений (17/20):**
- ✅ P0 Боковые полоски → фоновая подсветка
- ✅ P2 Hardcoded цвета → удалены fallback'и
- ❌ P1 Анимация ширины (ожидает `/impeccable optimize`)
- ❌ P1 ARIA-роли (ожидает `/impeccable harden`)
- ❌ P1 Явные лейблы (ожидает `/impeccable harden`)

**Улучшение:** +2 балла (15→17), Anti-Patterns поднялась с 2 на 3, Theming с 3 на 4.

---

## Положительные находки (что работает хорошо)

✅ **Отличное адаптивное поведение:** Breakpoint 640px чистый; мобильный layout переключается с таблицы на карточки без overflow'ов.

✅ **Сильная семантическая структура:** Таб-фильтры, поисковый инпут, date picker'ы логически сгруппированы.

✅ **Хорошая обработка ошибок:** Подтверждения удаления, toast'ы обратной связи, сообщения об ошибках при сбое операций.

✅ **Теперь 100% соблюдение дизайн-токенов:** Все цвета, интервалы, тени используют `globals.css` переменные (после удаления hardcoded fallback'ов).

✅ **Mobile-first CSS:** Media query'ы осознанные и верные.

---

## Рекомендуемые действия

1. **[P1] `/impeccable optimize`** — Замените `width` анимацию на `transform: scaleX()` (line 65).

2. **[P1] `/impeccable harden`** — Добавьте ARIA-роли в таб-виджет и явные `<label>` на форм-контролы.

3. **[P3] `/impeccable polish`** — Финальный quality pass после вышеперечисленного.

Вы можете запустить эти команды одну за другой, все сразу или в любом порядке.

**После исправлений запустите `/impeccable audit /sales` снова, чтобы увидеть оценку улучшиться до 19-20.**
