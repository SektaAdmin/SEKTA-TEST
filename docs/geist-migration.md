# Geist-міграція — трекер (посесійні етапи)

> **Протокол сесії, модельна матриця і гарди — навичка `.claude/skills/geist-session/SKILL.md`.**
> Нова сесія = «поїхали» = перший 🔲-етап; одна сесія = один етап. Примітка-blockquote
> під заголовком етапу визначає пакетування.
>
> Джерело правди: `docs/geist/` (сирий еталон Vercel/Geist: tokens.md, theming.md,
> components/*.md) + `docs/FRONTEND.md` (адаптовані примітиви — ModalShell/FormField/badges/tabs/токени).
>
> Статуси: 🔲 не перевірено · ✅ чисто · ⚠️ є розбіжності (див. колонку «Знахідки»)

## Конвенції прогону (устоялись на Етапі 0, нові знахідки міряти об них)

- skeleton → глобальний `.skeleton-bone` (локальні передруки shimmer видаляти);
- бейджі → глобальний `.badge` + модифікатори (є генеричні `.badge-success`/`.badge-danger`); мапери — тільки `lib/badges.ts` (`enrollmentBadgeClass`, `balanceClass`, `paymentClass`);
- таблиці → `.data-table`(-wrap); локальні оверрайди через `.wrap :global(.data-table) .cell` (без `!important`);
- типографіка: 12/13/14/16px → `--fs-xs/sm/base/md`; 20/24px → `--fs-lg/xl`;
- рухи: 0.1–0.15s → `--motion-fast`, 0.18–0.2s hover-переходи → `--motion-standard` (токени ВЖЕ містять easing — не дописувати `ease`);
- семантичний текст ≤18px → `--success-text/--danger-text/--warning-text` (заливки/бордери/іконки та текст >18px — raw `--success/--danger/--warning`);
- навмисно raw (НЕ чіпати): мікро-типографіка календарних сіток (10/11px), індикаторні акценти «зараз/сьогодні» на `--danger`, swipe-анімації зі своїм easing, динамічні inline-стилі позиціювання, `.paymentTabs` (рішення Сесії 12);
- `.page-foot` padding — у глобальному класі (inline-дублі прибрано).

## Етап 0 — ✅ виконано 2026-07-09 (сторінки 1–9)

| # | Маршрут | Файл | Статус | Дата | Знахідки |
|---|---|---|---|---|---|
| 1 | / | app/page.tsx | ✅ чисто | 2026-07-09 | Без UI — серверний redirect-компонент (auth-гейт), CSS/JSX відсутні |
| 2 | /dashboard | app/dashboard/page.tsx | ✅ чисто | 2026-07-09 | 5 знахідок виправлено: debtTable→.data-table (+локальні модифікатори через :global), geistBadge→.badge badge-type/badge-success (новий генеричний модифікатор), skeleton→global .skeleton-bone, 140ms→--motion-fast, CollapseHead h2 обгортає button (ARIA accordion) |
| 3 | /login | app/login/page.tsx | ✅ чисто | 2026-07-09 | 1 знахідку виправлено: `.logo` font-size 22px хардкод → var(--fs-lg) (L1-заголовок екрана) |
| 4 | /clients | app/clients/page.tsx | ✅ чисто | 2026-07-09 | Виправлено: skeleton→global .skeleton-bone; 14px→--fs-base (×10); .txPos/.txNeg→-text варіанти; 0.12s→--motion-fast (×5, width→--motion-standard); видалено dead-код editingClient. Відкладено (наскрізне): inline padding .page-foot (те саме на /sales,/journal,/audit) — винести в спільний клас при уніфікації |
| 5 | /clients/[id] | app/clients/[id]/page.tsx | ✅ чисто | 2026-07-09 | 9 знахідок виправлено: `.table`→глобальний .data-table (5 місць, uppercase-шапку прибрано; .tableWrap лишився як full-bleed скрол-зона картки); TONE_CLASS/.tone*→enrollmentBadgeClass() (канон-кольори з lib/badges.ts); balToneClass→balanceClass(); dead CSS видалено; 14px→--fs-base (×18); 0.15s/0.12s→--motion-fast; raw --success/--danger→-text (текст ≤18px; .msMetricAlert 24px навмисно raw); inline `<pre>`/порожня метрика→.credsBox/.msMetricEmpty; .confirmBox→calc(100%-32px)/max 360px як /sales |
| 6 | /schedule | app/schedule/page.tsx | ✅ чисто | 2026-07-09 | Аудит оркестратором (без субагента). Виправлено: 12/13/14/16px→--fs-xs/sm/base/md (×28); 0.1s/0.12s→--motion-fast; статус-тексти (SlotsFull/Free/Waitlist/Reserve/CancelledBadge)→-text варіанти. Навмисно raw: 10/11/15/17/28px мікро-типографіка сітки (поза шкалою); --danger на .nowLineTime/.mobileTlDayToday = індикаторний акцент «зараз/сьогодні» (збіг із кольором лінії); swipe-анімації 0.18s/0.2s зі своїм easing; inline-стилі в page.tsx — динамічне позиціювання. Спільні компоненти (MobileScheduleTimeline тощо) — поза скоупом рядка |
| 7 | /schedule/[classId] | app/schedule/[classId]/page.tsx | ✅ чисто | 2026-07-09 | Без UI — серверний redirect-стаб (`redirect('/schedule')`), CSS-модуля немає, локальних компонентів немає; реальний UI деталей заняття — ClassDetailModal (Етап V) |
| 8 | /schedule/templates | app/schedule/templates/page.tsx | ✅ чисто | 2026-07-09 | 3 знахідки виправлено: Список-вигляд → .data-table-wrap/.data-table (+.listCard з відступами як .gridCard); GridSkeleton → глобальний .skeleton-bone (skelPulse видалено); font-size хардкоди → токени (page ×11, SeriesModal ×6); бонус: .btnRowDel:hover текст → --danger-text. HallWeekGrid мікротипографія навмисно raw |
| 9 | /sales | app/sales/page.tsx | ✅ чисто | 2026-07-09 | Аудит оркестратором (без субагента). Виправлено: skeleton→глобальний .skeleton-bone (локальний shimmer видалено); 14/13px→--fs-base/--fs-sm; 0.12s→--motion-fast (і в SaleModal.module.css: 14px ×4, all 0.12s); danger-hover тексти (.filterClear/.btnDel)→--danger-text; inline-стилі рядка витрати/доходу→.mutedCell/.opLabel/.opIcon. Наскрізне закрито: padding .page-foot перенесено в глобальний клас, inline-дублі прибрано з /sales,/clients,/journal,/audit. Навмисно: .paymentTabs локальна (рішення Сесії 12); confirmBox 300px desktop + responsive mobile override — ок |

## Етап I — ✅ виконано 2026-07-09 (Фінанси, сторінки 10–13)

> /audit і /journal — майже дзеркальні модулі; всі три сторінки вже на глобальних `.data-table`/`.skeleton-bone`, лишились дрібні хардкоди (font-size px, 0.12s, raw --danger).
> Урок етапу: `!important` на td/th-класах знімати ТІЛЬКИ з рескоупом `.обгортка :global(.data-table) .клас` — гола `.class` (0,1,0) програє глобальному `.data-table td` (0,1,1); haiku на таке не здатен без точного рецепта (див. рядок 10).

| # | Маршрут | Файл | Статус | Дата | Знахідки |
|---|---|---|---|---|---|
| 10 | /accounting | app/accounting/page.tsx | ✅ чисто | 2026-07-09 | 4 знахідки виправлено: th/td-оверрайди (.thPrice/.thDeposit/.thAmt/.thMethod/.iconCell/.checkCell) → рескоуп `.tableDesktop :global(.data-table) .cell` без `!important`; `.amtExpense`/`.amtOtherIncome` (текст ≤18px) → `--danger-text`/`--success-text` (базовий клас для mobile-span + рескоуплений для td); font-size 12/13/14px → --fs-xs/sm/base (×21); мертвий inline flex-style на .cardList (page.tsx:32) видалено. Навмисно raw: `.balanceCardVal` 26px/22px — hero-number поза шкалою (прецедент .msMetricAlert); iconIncome/Expense та border .expenseCard — raw --success/--danger коректно (іконки/бордери). Пастка сесії: haiku-fixer зняв `!important` без рескоупу (програш специфічності `.data-table td`) → відкат + перезапуск на sonnet за гардом |
| 11 | /accounting/trainers | app/accounting/trainers/page.tsx | ✅ чисто | 2026-07-09 | Без UI — серверний redirect-стаб (`redirect('/accounting')`), CSS-модуля і локальних компонентів немає |
| 12 | /audit | app/audit/page.tsx | ✅ чисто | 2026-07-09 | 8 знахідок виправлено (haiku-fixer, гард пройдено): font-size 12/13/14px→--fs-xs/sm/base (×7: .topbarCount/.empty/.cardTitle/.cardDateTime/.cardMeta/.paginationInfo/.pageBtn); `.pageBtn` transition 0.12s→--motion-fast (×3); inline tabular-nums на td дати (page.tsx:258) + `.deliveredCell` → спільний `.tabularCell`. Навмисно raw: `.clearBtn:hover` var(--danger) — іконка X, не текст; skeleton-ширини — розмірні px; badge-* через глобальний .badge+lib/badges.ts; `.pagination` padding — локальний оверрайд ідентичний /journal (узгоджено в сесії 9) |
| 13 | /journal | app/journal/page.tsx | ✅ чисто | 2026-07-09 | 10 знахідок виправлено (haiku-fixer, гард пройдено), дзеркальні до /audit: font-size 12/13/14px→--fs-xs/sm/base (×7); `.pageBtn` transition 0.12s→--motion-fast (×3); inline tabular-nums на td часу → локальний `.tabularCell` (мірор audit-патерну); inline cursor:pointer на `<tr>` → селектор `.tableDesktop :global(.data-table) tbody tr`. Навмисно raw: skeleton-ширини (розмірні px), `.pagination` padding (ідентичний /audit), badge-class-cancelled/badge-completed — канонічні глобальні |

## Етап II — ✅ виконано 2026-07-10 (Довідники /settings, сторінки 14–22)

> №14–18 — redirect-стаби (аудит тривіальний, можна одним комітом). №19–21 рендеряться спільним рушієм `app/settings/_RefEntityPage.tsx` + `settings.module.css` — фактичний аудит ОДИН на трьох. №22 — окрема drag-drop сторінка (не через RefEntityPage).

| # | Маршрут | Файл | Статус | Дата | Знахідки |
|---|---|---|---|---|---|
| 14 | /halls | app/halls/page.tsx | ✅ чисто | 2026-07-10 | Без UI — redirect-стаб (`redirect('/settings?tab=halls')`), CSS/JSX відсутні |
| 15 | /trainers | app/trainers/page.tsx | ✅ чисто | 2026-07-10 | Без UI — redirect-стаб (`redirect('/settings?tab=trainers')`), CSS/JSX відсутні |
| 16 | /training-types | app/training-types/page.tsx | ✅ чисто | 2026-07-10 | Без UI — redirect-стаб (`redirect('/settings?tab=training-types')`), CSS/JSX відсутні |
| 17 | /tickets | app/tickets/page.tsx | ✅ чисто | 2026-07-10 | Без UI — redirect-стаб (`redirect('/settings?tab=tickets')`), CSS/JSX відсутні |
| 18 | /settings | app/settings/page.tsx | ✅ чисто | 2026-07-10 | Без UI — redirect-стаб (`redirect('/settings/tickets')`), CSS/JSX відсутні |
| 19 | /settings/halls | app/settings/halls/page.tsx | ✅ чисто | 2026-07-10 | Спільний рушій `_RefEntityPage.tsx`+`settings.module.css` (аудит один на №19–21). 18 правок виправлено (sonnet-fixer, список змішаний): font-size 13/14px→--fs-sm/--fs-base (×7); 0.12s→--motion-fast (.toggleBtn ×4, .editBtn/.restoreBtn ×3, .archiveToggle); chevron архіву 0.18s ease→--motion-standard; `.toggleFalse:hover`+`.toggleActiveFalse` color→--danger-text (текст ≤18px); `.mono`→рескоуп `.tableDesktop :global(.data-table) .mono` без `!important`; `.toggleActive*` — 6× `!important` зняті без рескоупу (hover-правила виключають active через `:not()`); `.restoreBtn` `opacity:1 !important` видалено як no-op (opacity батьківського td не скасовується дочірнім), `:disabled` без `!important`; `.skelCardName` — dead CSS видалено. Навмисно raw: `.toggleBtn` 11px — TRUE/FALSE мікро-лейбл поза шкалою (--fs-xs=12px); skeleton-ширини розмірні px; `_RefEntityPage.tsx:73` динамічна inline-width skeleton. Структурно чисто: ToggleBtns/tabNav/ArchiveSection — канонічний патерн /settings за FRONTEND.md (не дублюють .tabs-*); глобальні .badge/.data-table/.skeleton-bone використані коректно |
| 20 | /settings/tickets | app/settings/tickets/page.tsx | ✅ чисто | 2026-07-10 | Спільний рушій — виправлення у рядку №19; `tickets/page.tsx` структурно чисте (badge через глобальний `.badge badge-type`, без локальних дублів) |
| 21 | /settings/trainers | app/settings/trainers/page.tsx | ✅ чисто | 2026-07-10 | Спільний рушій — виправлення у рядку №19; `trainers/page.tsx` структурно чисте (badge через глобальний `.badge badge-completed`, `.dash`/`.handle` без дублів; TrainerModal — Етап VI, поза скоупом) |
| 22 | /settings/training-types | app/settings/training-types/page.tsx | ✅ чисто | 2026-07-10 | Окрема drag-drop сторінка (НЕ через `_RefEntityPage`). 6 знахідок виправлено (sonnet-fixer): font-size 13px→--fs-sm (`.saving`), 16px→--fs-md (`.handle`/`.handleMobile`); inline `style={{width:32}}` на `<th>` драг-колонки (skeleton+реальна шапка) → новий клас `.handleCol{width:32px}`; inline color на «—» (page.tsx:183) → `styles.dash` із settings.module.css. Навмисно raw: skeleton-ширини px (page.tsx:43,44,45,59); inline `opacity:0.65` на архівній mobile-картці — дзеркало канонічного патерну `_RefEntityPage.tsx:191` (лишено для консистентності, звести в клас при можливому спільному проході). Структурно чисто: ArchiveSection/ToggleBtns/tabNav/.data-table/.badge badge-type — канонічний патерн /settings; `.handle`/`.handleMobile`/`.dragOver`/`.saving` — власна drag-специфіка без дублювання Geist-примітивів |

## Етап III — ✅ виконано 2026-07-10 (Тренерський кабінет, сторінки 23–26)

> №24: власного CSS немає (спільний вже чистий schedule.module.css) — ревʼю лише TSX (TrainerSchedule.tsx 1426 р., тріаж оркестратором). №25 тягне ClientSessionsModal/CreateClientModal (локальні для зони — в скоупі рядка).
> Урок етапу: статичні inline-оверрайди СПІЛЬНОГО CSS-модуля виносити в локальний модуль зі скоупом `.shell .клас` (0,2,0) — гола локальна `.клас` (0,1,0) залежить від порядку бандла проти класів спільного модуля; заодно перевіряти inline CSS-змінні на мертвих споживачів (`--sidebar-w: 0px` без Sidebar/.page-main у піддереві).

| # | Маршрут | Файл | Статус | Дата | Знахідки |
|---|---|---|---|---|---|
| 23 | /trainer | app/trainer/page.tsx | ✅ чисто | 2026-07-10 | 10 знахідок виправлено (haiku-fixer, гард пройдено), всі в trainer.module.css: font-size 12/13/14px→--fs-xs/sm/base (×6); 0.12s→--motion-fast (×2, .tgDisconnectBtn/.menuItem); `color:var(--danger)` на тексті ≤18px (.tgDisconnectBtn:hover, .menuLogout)→--danger-text. Навмисно raw: `.avatar` 22px (hero-number, прецедент .msMetricAlert); `.profileName` 18px і `.menuItem` 15px — поза шкалою без токена-відповідника (прецедент 15/17px /schedule); `.menuLogout .menuIcon` var(--danger) (іконка, не текст); `TrainerHome.tsx:42` inline `background:avatarColor(...)` (динамічне значення per-тренер). Структурно чисто: модалок/форм/таблиць/skeleton немає — ModalShell/FormField/badges.ts/.data-table не застосовні; `btn-primary` перевикористано коректно; меню-лаунчер — власний патерн кабінету |
| 24 | /trainer/schedule | app/trainer/schedule/page.tsx | ✅ чисто | 2026-07-10 | Аудит оркестратором (без субагента, за приміткою етапу). З 21 inline-стилю прескану: 14 — динамічне позиціювання (tooltip pos, card top/height/lanes, --hour-h/--card-color, gridTemplateColumns від weekDays, nowTop/hourHeight) — навмисно raw; 7 статичних винесено в НОВИЙ локальний `trainerSchedule.module.css` зі скоупом `.shell` (специфічність (0,2,0) над класами спільного модуля без `!important`): .mainNoSidebar (margin-left/padding-bottom 0), .topbarCompact (48px), .menuBlock, .navCenter, .menuLabel (fontSize 13→--fs-sm), .hallChips; мертвий inline `'--sidebar-w':'0px'` (L1124) видалено — споживачів змінної у піддереві немає (Sidebar/.page-main не рендеряться). Спільний schedule.module.css не чіпано (чистий за №6). Структурно чисто: btn-primary/btn-secondary/tabs-segmented/filterChips — глобальні примітиви коректно; ClassModal/ClassDetailModal — спільні (Етапи V–VI) |
| 25 | /trainer/clients | app/trainer/clients/page.tsx | ✅ чисто | 2026-07-10 | 10 знахідок виправлено (haiku-fixer, гард пройдено), усі механічні (`clients.module.css`): font-size 13/14/16px→--fs-sm/base/md (×8: .backBtn,.searchClear,.balanceSessionsUnit,.modalSectionLabel L25,90,188,240 = --fs-sm; .searchInput,.empty,.balanceEmpty L73,151,254 = --fs-base; .depositValue L279 = --fs-md); transition 0.12s/0.1s→--motion-fast (×2: .card L130, .fab L177). Навмисно raw (підтверджено): font-size поза шкалою 15px×5 (.topbarTitle,.cardName,.balanceRowLabel,.balanceZero,.depositLabel — прецедент 15/17px /schedule і /trainer, рядок 23) + 28px (.balanceSessionsNum — hero-number, прецедент .msMetricAlert); TrainerClients.tsx:27 inline skeleton-width (динамічна ширина рядка). Структурно чисто: CreateClientModal — канонічний scaffold (ModalShell+ModalFooter+FormField+RHF+VM), перевикористовує ClientModal.module.css (.row/.error) замість дублю; ClientSessionsModal — ModalShell footer={null} коректно, .balanceBlock/.depositRow — навмисний hero-card дизайн (дзеркало /client/subscriptions, коментар у CSS: «нейтраль > семантика на read-only поверхні»), локальний 3-стан .depositValue/-Zero/-Neg — задокументоване відхилення від balanceClass(), не сплутане дублювання; список у TrainerClients.tsx коректно використовує balanceClass()/ticketTypeNominativeLabel() з lib/badges.ts; skeleton — глобальний .skeleton-bone скрізь |
| 26 | /trainer/my | app/trainer/my/page.tsx | ✅ чисто | 2026-07-10 | 10 знахідок виправлено (haiku-fixer, гард пройдено), усі в `my.module.css`: font-size 12/13/14px→--fs-xs/sm/base (×5: L301 cardMeta, L25 backBtn, L44 todayBtn, L292 cardTitle, L362 emptyDay); 0.1–0.12s transitions→--motion-fast (×3: L72 .fab, L125 .weekDay, L263 .card); border-radius 6px→--radius-sm (L255 .card); `.cardCancelledBadge` color:var(--danger) (L352, текст «Скасовано» ≤18px, НЕ індикатор «зараз/сьогодні»)→--danger-text (промотано з «механічне: ні» після тріажу). Навмисно raw (підтверджено): font-size поза шкалою 10/11/15/17px×9 — мікро-типографія тайм-лайн сітки (прецедент /schedule рядок 6, /trainer рядок 23, /trainer/clients рядок 25); `.slideLeft`/`.slideRight` 0.2s ease (L112-113) — swipe-анімації зі своїм easing; `.weekDayToday .weekDayLabel` color:var(--danger) (L165) — індикаторний акцент «сьогодні» (на відміну від L352); 3 inline-стилі TrainerMyClasses.tsx (L164 hourRow top, L172 nowLine top, L192 card top/height/--card-color) — динамічне позиціювання. Структурно чисто: skeleton відсутній (серверний фетч без client-side loading), `.cardCancelledBadge`/`.fab` — локальні калькові патерни, узгоджені з прецедентом `/schedule` (`.mobileTlCardCancelled` тощо), не дублюють `.badge`+lib/badges.ts; ClassDetailModal/ClassModal — канонічні спільні компоненти (поза скоупом рядка) |

## Етап IV — ✅ виконано 2026-07-10 (Клієнтський кабінет, сторінки 27–30)

> Усі 4 сторінки (+/client/visits/[id]) ділять ОДИН `app/client/client.module.css` (1463 р.) — CSS-прохід один на етап, далі по-сторінково TSX. Відомий дубль: локальний `@keyframes skelPulse` (L120-123) замість глобального `.skeleton-bone`.
> Урок етапу: для зон зі спільним модулем маршрут «migrator тріажить УВЕСЬ CSS одним прогоном (+ перша сторінка), далі TSX-огляди решти сторінок оркестратором» — один прескан, один fixer-прогін, нуль дублювання. Локальні родини кабінету (аватар-чипи `.bookTrainerChip`, sticky flex:1 `.visitTabs`, hero-блоки, `.visitHistoryBadge` із 3-станом балансу) — навмисні патерни, не дрейф глобальних примітивів.

| # | Маршрут | Файл | Статус | Дата | Знахідки |
|---|---|---|---|---|---|
| 27 | /client | app/client/page.tsx | ✅ чисто | 2026-07-10 | **CSS-прохід спільного `client.module.css` за прецедентом рядка 19** (сторінки 27–30 ділять модуль). 16 пунктів виправлено (sonnet-fixer, список змішаний): font-size 12/13/14/16/20px→--fs-xs/sm/base/md/lg (×48, увесь модуль); `.bookTodayBtn` transition 0.12s→--motion-fast; локальний `@keyframes skelPulse`+`.skel` видалено → глобальний `.skeleton-bone` у 5 loading.tsx (client/, schedule/, subscriptions/, visits/, visits/[id]/; `.skelCard`/`.skelBlock` лишились як розмірні модифікатори); raw колір на тексті ≤18px→-text (×13: `.visitHistoryBadge.visitTimer*`-комбо, `.cancelBtn`, `.confirmRuleWarn`, `.confirmReserve`, `.bookSlotTag`-родина); бонус-катч поза пресканом: `VisitDetail.tsx:82` inline `color:'#fff'`→`var(--white)`. **Структурне (тріаж оркестратором):** локальний `.badge` (вжиток лише `VisitDetail.tsx:94`) видалено → глобальний `badge badge-class-cancelled` (дзеркало сусіднього `enrollmentBadgeClass` L91); dead CSS `.visitTimerAttended/Cancelled/Noshow` (L609-625) видалено — тон-класи завжди в парі з `.visitHistoryBadge` (комбо-правила лишились, CSS-module експорт імен не ламається). Навмисно raw (підтверджено, прецедент /schedule р.6 + /trainer р.23,25,26): font-size поза шкалою 10/11/15/17/18/22/28/30px (×19 — avatar-родина, hero-число `.balanceSessionsNum`, календарна мікротипографія); `--danger` на `.bookDayToday` (індикатор «сьогодні»); swipe `.bookDaysSlideLeft/Right` 0.2s; коментар-хекс L220 (документація); динамічні inline (avatarColor ×2, skeleton-розміри); `layout.tsx:8 themeColor` — Next.js Metadata вимагає літерал. Сторінка 27 (page.tsx/ClientHome.tsx/layout.tsx/loading.tsx) структурно чиста: `.shell`+`CabinetHeader` коректні, форм/модалок/таблиць немає |
| 28 | /client/schedule | app/client/schedule/page.tsx | ✅ чисто | 2026-07-10 | TSX-огляд оркестратором (спільний CSS закрито рядком 27, власного немає). Правок нуль. Структурно чисто: модалка підтвердження запису — канонічні ModalShell+ModalFooter; skeleton — глобальний `.skeleton-bone` + локальні розмірні модифікатори (`.skelDayNum`/`.skelSlot*`); словники — `ticketTypeShortLabel`/`ticketTypeNominativeLabel`/`enrollmentStatusLabel` з lib/badges.ts. Навмисно raw: `ClientSchedule.tsx:52` inline динамічна ширина skeleton; свайп `.bookDaysSlide*` (спільна родина, рядок 27). `.bookTrainerChip` — власний аватар-чип кабінету (ініціали всередині, інша анатомія) — НЕ дубль глобального `.filterChip`. `page.tsx` — серверний prefetch без UI |
| 29 | /client/subscriptions | app/client/subscriptions/page.tsx | ✅ чисто | 2026-07-10 | TSX-огляд оркестратором. Правок нуль: жодного inline-стилю (loading.tsx мігровано на `.skeleton-bone` рядком 27); платіжні бейджі через `clientPaymentClass()`/`clientPaymentLabel()` з lib/badges.ts; hero-блок балансу `.balanceBlock`/`.depositRow` — задокументований дизайн (дзеркало ClientSessionsModal, рядок 25, «нейтраль > семантика»); іконки за словником проєкту (16×16, stroke 1.4); `SectionError` з кнопкою «Оновити» — локальний патерн секційної помилки, ок. `page.tsx` — серверний prefetch без UI |
| 30 | /client/visits | app/client/visits/page.tsx | ✅ чисто | 2026-07-10 | +visits/[id]/VisitDetail.tsx. TSX-огляд оркестратором. Правки VisitDetail зроблено в коміті рядка 27 (`color:'#fff'`→`var(--white)`, локальний `.badge`→глобальний `badge badge-class-cancelled`) — після них уся сторінка канонічна: hero-статус через `enrollmentBadgeClass()`, модалка скасування — ModalShell+ModalFooter `danger`. ClientVisits: статуси через `enrollmentBadge()` з lib/badges.ts + локальна родина `.visitHistoryBadge` (навмисне відхилення: 3-стан балансу `visitBadgeBalance*`, якого немає в глобальних `.badge-*`, і neutral-override для cancelled у комбо — «нейтраль > семантика»); `.visitTabs`/`.visitTab` — НЕ дубль глобального `.tabs-underline` (sticky-шапка скрол-контейнера + рівноширинні flex:1 таби — кабінетний варіант, прецедент локальних патернів); WeekStrip — спільний патерн із /client/schedule (свайп/сітка, ті самі `.bookDay*`). `page.tsx`/`[id]/page.tsx`/`not-found.tsx` — серверні обгортки без власних стилів |

## Етап V — ✅ виконано 2026-07-10 (ClassDetailModal)

> Найбільший спільний компонент (45КБ TSX + 20КБ CSS), використовується адмінкою/тренером/дашбордом — окрема сесія.
> Урок етапу: `!important` у ЛОКАЛЬНІЙ табличці (не .data-table) знімати компаунд-селектором `.table td.клас` — рецепт рескоупу `:global(.data-table)` з Етапу I тут не застосовний; хекси всередині CSS-коментарів прескан ловить як «хардкоди» — false positive, фільтрувати на тріажі.

| # | Скоуп | Файл | Статус | Дата | Знахідки |
|---|---|---|---|---|---|
| 31 | ClassDetailModal | components/ClassDetailModal.tsx + .module.css | ✅ чисто | 2026-07-10 | 33 правки виправлено (sonnet-fixer, список змішаний): font-size 12/13/14px→--fs-xs/sm/base (×23, включно з media-блоками); transition 0.12s→--motion-fast (×3 в одному правилі .btnRowEdit/.btnRowDel); raw --danger на тексті ≤18px→--danger-text (×4: .enrollError, .rowError, hover-текст .btnRowDel/.btnDeleteGhost — прецедент рядків 8–9; border/background лишено raw); 3 `!important` зняті компаунд-селектором `.table th.thNum`/`.table td.actionsCell` (специфічність 0,2,1 над `.table td` 0,1,1) — НЕ рескоупом .data-table, бо таблиця навмисно локальна (FRONTEND.md:44: складна мобільна card-розкладка grid-template-areas); мобільний оверрайд виграє каскадом (пізніше в файлі, рівна специфічність); статичні inline TSX→класи (.cancelledInline L469, .sourceHint L582); dead CSS `.hidden` видалено. Навмисно raw (підтверджено): 3 «хардкоди» = хекси в CSS-коментарях (false positive прескану); .calloutIcon var(--warning) — іконка-гліф ⚠; font-size 11/11/15px (.rowNum/.rowError/моб. .clientLink) — мікро-типографія поза шкалою; 15 skeleton inline-ширин; Icon flexShrink:0 у бейджі (прецедент ClientDetailClient:893; глобального правила `.badge svg` НЕМАЄ — inline потрібен). Структурно канонічно (НЕ дрейф): ModalShell size="detail" + footer={null} + власний .footerActions (3 різні дії ≠ семантика ModalFooter); `<table styles.table>` без .data-table — навмисно за FRONTEND.md:44; ActionSelect; enrollmentBadge/enrollmentBadgeClass/balanceClass/cancelSourceSuffix з lib/badges.ts; CopyButton; .skeleton-bone; detailsCard DL (канон b9cc20a); confirmOverlay/Dialog (еталон /sales С4) |

## Етап VI — 🔲 Спільні компоненти

> Цілі-примітиви НЕ аудитувати (вони і є еталон): ModalShell, FormField, ModalFooter. SaleModal/SeriesModal вже пройдені (Етап 0, рядки 8–9). HallWeekGrid — частково (мікротипографія raw).

| # | Скоуп | Файли | Статус | Дата | Знахідки |
|---|---|---|---|---|---|
| 32 | Навігація | components/Sidebar, BottomNav, CabinetHeader | ⚠️ 10 знахідок | 2026-07-10 | Тріаж (готові до fixer): 10 механічних — font-size→токен ×8 (Sidebar.module.css:16→--fs-md, :42/:83/:128→--fs-base, :112→--fs-sm; BottomNav.module.css:78→--fs-base, :92/:117→--fs-md); transition 0.12s→--motion-fast ×2 (Sidebar.module.css:48,130). Навмисно raw (підтверджено тріажем): `BottomNav.module.css:52` `.label` 10px — мікро-типографія таб-бару нижньої нав (аналог календарного винятку з шапки конвенцій, менших токенів за --fs-sm немає); `Sidebar.module.css:133` `.logout:hover{color:var(--danger)}` і `BottomNav.module.css:123` `.drawerLogout:active{color:var(--danger)}` — одне CSS-правило керує одразу іконкою (`LogoutIcon` `stroke=currentColor`) і текстовим вузлом «Вийти» без окремого `<span>`; поділ на `--danger-text` вимагав би обгортання тексту в span (DOM-зміна) — залишено raw як нероздільний контрол. Структурно чисто: `CabinetHeader.tsx` `.back`/logout перевикористовують глобальний `btn-secondary` (не дублюють кнопку, `docs/FRONTEND.md`), fs-токени й `--control-h` вже застосовані; `BottomNav` `.drawerOverlay`/`.drawer` — власний легкий action-sheet (НЕ ModalShell — без title/footer), навмисно z-index `199<200` щоб бар лишався видимим над затемненням, перевикористовує спільні `bottomSheetIn`/`overlayIn` keyframes + `--motion-fast`/`--radius`/`--border-hover` без реізобретення; жодних локальних `.badge`/дублів `.data-table`/`.tabs-*`/`.filterChip*`. Дрібниця: `BottomNav.module.css:43` `.moreBtn{font-size:inherit}` — імовірний no-op (успадкований font-size ніде не відрізняється), не потребує окремого фіксу |
| 33 | Модалки сутностей | components/ClassModal, ClientModal, EnrollClientModal, SlotFinderModal, StudioExpenseModal, TicketModal, TrainerModal, HallModal, TrainingTypeModal | ✅ чисто | 2026-07-10 | 35 знахідок виправлено (sonnet-fixer, список змішаний, корекції оркестратора перед фіксом): font-size→токен ×27 (SlotFinderModal.module.css ×7: :15,21→--fs-sm,:55,67,81→--fs-sm,:94,147→--fs-xs,:173→--fs-base; TrainerModal.module.css:2,21,27,39→--fs-base×3/--fs-sm×1 — прескан НЕ застарів, попередній тріаж помилково вважав файл 8-рядковим із самим `.error`, оркестратор перечитав: 43 рядки, .cabinet/.cabinetLabel/.cabinetHint/.creds існують і використовуються TrainerModal.tsx:174-223, правки застосовано штатно; ClassModal.module.css ×5:13,38,53,83,89→--fs-base; EnrollClientModal.module.css ×6:9,17,58,65,70,85→--fs-base; StudioExpenseModal.module.css:14,45→--fs-base; TicketModal/TrainerModal/HallModal/TrainingTypeModal `.error`×4→--fs-base). raw --danger→--danger-text на тексті ≤18px ×7 (ClassModal:14, ClientModal:13, EnrollClientModal:75 `.full`, StudioExpenseModal:29(в межах структурної правки)+46, TicketModal:13, TrainerModal:3, TrainingTypeModal:3; HallModal.module.css:3 вже був --danger-text на момент фіксу — прескан застарів для цього конкретного рядка, fixer коректно пропустив, лишив тільки fs на рядку 2). **Структурне:** StudioExpenseModal.module.css:26-42 `.dirBtnExpense/.dirBtnIncome/.dirBtnTransfer` — 9 `!important` зняті заміною СЕЛЕКТОРА на компаунд `.dirBtn.dirBtnExpense` (специфічність 0,2,0, рівна з `.dirBtn:hover`, виграє за порядком джерела — той самий рецепт що Етап V рядок 31/Етап VI рядок 35, а не рескоуп `.data-table` Етапу I). Відхилено як false positive (підтверджено): 5 «мертвих» класів SlotFinderModal (.cell_free/.cell_hall_busy/.cell_past/.cell_selftraining/.cell_trainer_busy) — живі, референс динамічний темплейт-літерал `styles[`cell_${status}`]` (SlotFinderModal.tsx:331), прескан не бачить; 2 `<table>` без .data-table (ClientModal.tsx:37,242 `.historyTable`) — навмисний компактний варіант (FRONTEND.md:44); 4 fs поза шкалою (ClientModal.module.css:57,92,105; SlotFinderModal.module.css:105) — мікротипографія щільних таблиць/сітки; 15 inline-стилів TSX — skeleton/динамічне позиціювання. Сумнівне вирішено оркестратором: `ClassModal.module.css` `.toggleRow/.toggleBtn/.toggleActive` (2-позиційний inset pill-toggle) і `StudioExpenseModal.module.css` `.directionRow/.dirBtn*` (3-позиційний) звірено з глобальним `.tabs-segmented`(app/globals.css:478-493, flat divided) — ПІДТВЕРДЖЕНО різні візуальні патерни, не дублювання примітиву, не зводити. Відкладено (беклог, не блокує чистоту рядка): `.error`-банер майже побайтово продубльований у 7/9 модулів — кандидат на спільний клас у globals.css при наступному проході. Структурно чисто: усі 9 модалок коректно перевикористовують ModalShell+FormField+ModalFooter (HallModal/StudioExpenseModal — легкий non-RHF, консистентний); SocialHandleInput перевикористано (Client/TrainerModal); TrainerModal бейдж badge badge-completed — канонічний; SlotFinderModal коректно перевикористовує ClientSearchCombobox, ModalShell size="detail" |
| 34 | Дата-пікери | components/CalendarPopover, SalesDateRangePicker, DatePicker, DateTimeInput, DateTimePicker | ⚠️ 24 знахідки | 2026-07-10 | Тріаж (готово до fixer): 15 механічних із прескану — fs→--fs-base ×14 (CalendarPopover.module.css:30,84; SalesDateRangePicker.module.css:14,82,126,176,263,278,294,308,399; DatePicker.module.css:14; DateTimePicker.module.css:62,79); кирилиця SalesDateRangePicker.tsx:201. Навмисно raw підтверджено: fs поза шкалою 10/10/11px (CalendarPopover.module.css:67, SalesDateRangePicker.module.css:158, DateTimePicker.module.css:49 — мікротипографія сітки); inline CalendarPopover.tsx:70 (динамічне позиціювання). !important ×2 (CalendarPopover.module.css:84, SalesDateRangePicker.module.css:176) — рецепт прескану (рескоуп `:global(.data-table)`) тут не пасує буквально (немає data-table, це `.day`-кнопка попапу) → структурне, з'ясувати джерело конфлікту специфічності перед фіксом (імовірно знімається без рескоупу, перевірити візуально). Dead CSS ×4 (.classDot/.dayInWeek/.dayWeekStart/.dayWeekEnd) підтверджено невживаними в межах цих 10 файлів, АЛЕ FRONTEND.md:13 документує в родині «Date*» ще й DateRangePicker+MonthNav — відсутні і в цьому прескані, і в жодному рядку Етапу VI (32–35); назва dayInWeek/dayWeekStart/dayWeekEnd натякає на тижневий рендж → імовірний споживач DateRangePicker.tsx поза скоупом. НЕ видаляти без audit-page.sh usages по всьому репо + розширення скоупу трекера на DateRangePicker/MonthNav. **Структурне (нове):** (1) SalesDateRangePicker.module.css побайтово дублює анатомію CalendarPopover.module.css (.calHeader/.monthLabel/.monthNav/.grid/.weekday/.day/.dayNum/.dayOutside/.todayDot — ідентичні правила, включно з тим самим `!important`) замість імпорту `calStyles` (CalendarPopover.tsx:7 `export { styles as calStyles }`), який коректно перевикористовують DatePicker.tsx:5,53-61 і DateTimePicker.tsx:6,157-165 — SalesDateRangePicker має лишити локальними тільки range-специфічні класи (.dayInRange/.dayRangeStart/.dayRangeEnd/.dayEndpoint). (2) Розбіжна елевація попапів у одній родині: CalendarPopover.module.css:1-10 — еталон «Shadow Menu» Сесії 3 (`box-shadow:var(--shadow-menu)`, без бордера, той самий що .fs-content/FilterSelect); SalesDateRangePicker.module.css:47-56 — старіший рецепт `border:1px solid var(--border-strong)` + `box-shadow:var(--shadow-md),var(--shadow-sm)`. Токени валідні, але дрейф консистентності — звести SalesDateRangePicker до `var(--shadow-menu)` без ручного border. (3) DateTimeInput.module.css:13-27 і DateTimePicker.module.css:16-30 — побайтово ідентичний `.calendarBtn`, але дві розбіжні UX-стратегії відкриття (DateTimeInput: прихований нативний `<input type=datetime-local>` з opacity:0 поверх кнопки; DateTimePicker: кастомний CalendarPopover) — сумнівне, потребує рішення: задокументувати навмисний легкий/важкий варіант в FRONTEND.md чи звести до одного. Позитив: спільна анатомія CalendarPopover↔DatePicker↔DateTimePicker через `calStyles` працює коректно; z-index:400 обох попаверів відповідає FRONTEND.md:25; ширина попапу 256px відповідає історичному рішенню (CalendarPopover sizing) |
| 35 | ui/* та інше | components/ui/{ActionSelect, CopyButton, Pagination, FilterSelect, SocialHandleInput}, features/ClientSearchCombobox, ScheduleRightPanel, StudioContactIcons | ✅ чисто | 2026-07-10 | 19 знахідок виправлено: font-size поза шкалою→токен ×15 (ActionSelect.module.css:12,60→--fs-sm,:30→--fs-base; Pagination.module.css:15,26,50,71→--fs-base; SocialHandleInput.module.css:20,35→--fs-base; ClientSearchCombobox.module.css:10,41,62→--fs-base; ScheduleRightPanel.module.css:25→--fs-base); transition→--motion-fast ×3 (Pagination.module.css:30; SocialHandleInput.module.css:33; ClientSearchCombobox.module.css:43). Структурне ×2 (обидва без рескоупу .data-table — компонент не в таблиці): ScheduleRightPanel.module.css:79 `.day` — !important був мертвим залишком, прибрано разом із заміною на --fs-base; Pagination.module.css:62-67 `.btnActive` — справжня причина не .data-table (fixer виявив: Pagination рендериться СУСІДОМ .data-table-wrap, а не всередині — рескоуп ніколи б не заматчився, регресія підсвітки активної сторінки), а програш специфічності `.btn:hover:not(:disabled)` (0,3,0); виправлено оркестратором: !important знято, доданий компаунд `.btn.btnActive:hover{...}` (теж 0,3,0, пізніше в файлі — виграє каскадом). Навмисно raw (підтверджено): ScheduleRightPanel.module.css:62 `.weekday` 10px — мікротипографія сітки; ClientSearchCombobox.module.css:17 border-color:var(--danger) — бордер не текст; CopyButton.module.css:49 0.2s ease-in-out — задокументований точний Geist-спек 200ms (FRONTEND.md:31), не --motion-standard. Відкладено (не критично, наступний прохід): SocialHandleInput.module.css:4 `.field label` 11px і ClientSearchCombobox.module.css:54 `.phone` 11px — поза шкалою без чіткого винятку, потрібен --fs-xs-токен або підтвердження патерну; CopyButton.module.css:32 `.sm{height:26px}` — звірити з --btn-sm-* токенами в globals.css. Структурно чисто: ActionSelect (Radix SelectPrimitive, FRONTEND.md:28, навмисно не shadcn), FilterSelect (inline Radix, без .module.css навмисно), CopyButton (єдине джерело копіювання), ClientSearchCombobox (власний ARIA-combobox, не дублює ActionSelect/FilterSelect), ScheduleRightPanel (власний міні-календар, не дублює CalendarPopover/DatePicker), StudioContactIcons (styles-проп навмисно) |
